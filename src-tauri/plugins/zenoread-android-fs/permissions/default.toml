"$schema" = "schemas/schema.json"

[default]
description = """
Enables the Android SAF picker commands.

#### Granted Permissions

- `allow-pick-file`
- `allow-check-persisted`
"""
permissions = ["allow-pick-file", "allow-check-persisted"]
